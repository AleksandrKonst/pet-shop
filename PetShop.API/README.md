# PetShop API

ASP.NET Core Web API для интернет-магазина зоотоваров с JWT авторизацией.

## 🚀 Функционал

### Авторизация
- Регистрация пользователей
- Вход в систему
- JWT токены для аутентификации
- Две роли: **User** и **Manager**

### Роли
- **User** - может просматривать товары, добавлять их в корзину и покупать
- **Manager** - может добавлять/редактировать/удалять товары и категории

### API Endpoints

#### Авторизация
- `POST /api/auth/register` - регистрация
- `POST /api/auth/login` - вход

#### Категории
- `GET /api/categories` - список категорий
- `GET /api/categories/{id}` - получить категорию
- `POST /api/categories` - создать категорию (Manager)
- `PUT /api/categories/{id}` - обновить категорию (Manager)
- `DELETE /api/categories/{id}` - удалить категорию (Manager)

#### Товары
- `GET /api/products` - список товаров (с фильтром по категории)
- `GET /api/products/{id}` - получить товар
- `POST /api/products` - создать товар (Manager)
- `PUT /api/products/{id}` - обновить товар (Manager)
- `DELETE /api/products/{id}` - удалить товар (Manager)

#### Корзина
- `GET /api/cart` - получить корзину (User)
- `POST /api/cart` - добавить товар в корзину (User)
- `PUT /api/cart/{id}` - обновить количество (User)
- `DELETE /api/cart/{id}` - удалить товар из корзины (User)
- `DELETE /api/cart` - очистить корзину (User)

#### Заказы
- `GET /api/orders` - список заказов пользователя (User)
- `GET /api/orders/{id}` - получить заказ (User)
- `POST /api/orders` - создать заказ из корзины (User)

## 📦 Установка

1. Убедитесь, что установлен .NET 8 SDK:
```bash
dotnet --version
```

2. Перейдите в директорию проекта:
```bash
cd PetShop.API
```

3. Восстановите зависимости:
```bash
dotnet restore
```

4. Настройте строку подключения к БД в `appsettings.json`:
```json
"ConnectionStrings": {
  "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=PetShopDB;Trusted_Connection=true;TrustServerCertificate=true;"
}
```

5. База данных создастся автоматически при первом запуске с тестовыми данными

## 🏃 Запуск

```bash
dotnet run
```

API будет доступен по адресу: `http://localhost:5000`

Swagger документация: `http://localhost:5000/swagger`

## 🧪 Тестирование

### Создание тестовых пользователей

**Manager:**
```json
POST /api/auth/register
{
  "username": "manager",
  "email": "manager@petshop.com",
  "password": "Manager123!",
  "role": "Manager"
}
```

**User:**
```json
POST /api/auth/register
{
  "username": "user",
  "email": "user@petshop.com",
  "password": "User123!",
  "role": "User"
}
```

### Использование токена

После успешной регистрации/входа вы получите JWT токен. Используйте его в заголовке запросов:

```
Authorization: Bearer {ваш-токен}
```

В Swagger нажмите кнопку "Authorize" и введите токен.

## 🗄️ База данных

Проект использует SQL Server LocalDB. При первом запуске база данных создастся автоматически с тестовыми данными:

- 3 категории (Товары для собак, кошек, птиц)
- 8 товаров в разных категориях

## 🔧 Технологии

- ASP.NET Core 8.0
- Entity Framework Core
- SQL Server
- JWT Authentication
- BCrypt для хеширования паролей
- Swagger/OpenAPI

## 📝 Примечания

- JWT токены действительны 24 часа
- Пароли хешируются с использованием BCrypt
- CORS настроен для работы с React приложением на `http://localhost:3000`


